{{- if .Values.coreWebapi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "poslite-horustech.fullname" . }}-core-webapi
  labels:
    {{- include "poslite-horustech.labels" . | nindent 4 }}
    component: core-webapi
spec:
  replicas: {{ .Values.coreWebapi.replicas }}
  selector:
    matchLabels:
      {{- include "poslite-horustech.selectorLabels" . | nindent 6 }}
      component: core-webapi
  template:
    metadata:
      labels:
        {{- include "poslite-horustech.selectorLabels" . | nindent 8 }}
        component: core-webapi
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
      - name: webapi
        image: "{{ .Values.imageRegistry }}/{{ .Values.imageRepository }}/{{ .Values.coreWebapi.image.repository }}:{{ .Values.coreWebapi.image.tag }}"
        imagePullPolicy: {{ .Values.pullPolicy | default "Always" }}
        ports:
        - name: http
          containerPort: {{ .Values.coreWebapi.port }}
          hostPort: {{ .Values.coreWebapi.hostPort }}
          protocol: TCP
        env:
        - name: TZ
          value: {{ .Values.config.timezone }}
        - name: ASPNETCORE_HTTP_PORTS
          value: "{{ .Values.coreWebapi.port }}"
        - name: GuardApi:Url
          value: "http://{{ include "poslite-horustech.guardApiService" . }}:{{ .Values.guardApi.port }}/guard/status"
        - name: CompanyId
          value: "{{ .Values.config.companyId }}"
        - name: ConnectionStrings:CoreDbContext
          valueFrom:
            secretKeyRef:
              name: {{ include "poslite-horustech.fullname" . }}-config
              key: db-connection-string
        - name: ConnectionStrings:ApplicationIdentityDbContext
          valueFrom:
            secretKeyRef:
              name: {{ include "poslite-horustech.fullname" . }}-config
              key: db-connection-string
        - name: HorustechApiClient:BaseUrl
          value: "http://{{ include "poslite-horustech.webapiService" . }}:{{ .Values.webapi.port }}/"
        - name: HorustechApiClient:ApiKey
          value: {{ .Values.config.horustechApiKey | quote }}
        - name: WebEventsClient:BaseUrl
          value: "http://{{ include "poslite-horustech.coreWebeventsService" . }}:{{ .Values.coreWebevents.port }}/"
        - name: WebEventsClient:ApiKey
          value: {{ .Values.config.coreApiKey | quote }}
        - name: IerpApi:BaseUrl
          valueFrom:
            configMapKeyRef:
              name: {{ include "poslite-horustech.fullname" . }}-config
              key: ierp-url
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.coreWebapi.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.coreWebapi.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
      hostname: horustech-core-webapi
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

---
{{- if .Values.coreWebapi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "poslite-horustech.coreWebapiService" . }}
  labels:
    {{- include "poslite-horustech.labels" . | nindent 4 }}
    component: core-webapi
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.coreWebapi.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "poslite-horustech.selectorLabels" . | nindent 4 }}
    component: core-webapi
{{- end }}
